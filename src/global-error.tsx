import React, { FC } from "react";

// Global error shows a global custom error if it happens in the root of project at root layout
const GlobalErrorPage: FC = () => {
  return <div>A global error occured</div>;
};

export default GlobalErrorPage;
